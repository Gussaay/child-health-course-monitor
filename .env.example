# Copy to .env for local development. The real .env is not committed.
# The app version is written by vite.config.js from package.json, so this is
# only a fallback for tooling that reads the variable directly.
VITE_APP_VERSION=0.0.0
