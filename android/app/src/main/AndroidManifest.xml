<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
          allowBackup was "true". Android Auto Backup then saved the Capgo
          bundle folder and its "next bundle" pointer to Google Drive. After a
          reinstall or a new phone, Android restored those files next to a
          DIFFERENT APK, so the app started an old JavaScript bundle. That is
          one way users end up on the previous version. It also restored the
          old WebView storage, which invalidates the saved login session.
        -->

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name="com.nchp.monitoring.app.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          GenericIdpActivity is only needed for the WEB redirect sign-in flow
          (signInWithRedirect). This app signs in natively with
          @capacitor-firebase/authentication, so it is not used.

          It is also risky here: its host is com.nchp.monitoring.app, which is
          exactly the Capacitor server hostname, so any in-app navigation to
          /__/auth/handler is pulled out of the app into this activity.
          Combined with launchMode="singleTask" on MainActivity, that can
          restart the activity and drop the session.

          Delete this block if native sign-in is the only method you use.
          Keep it only if some users still sign in through a web redirect.
        -->
        <activity
            android:name="com.google.firebase.auth.internal.GenericIdpActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="com.nchp.monitoring.app"
                    android:pathPrefix="/__/auth/handler"
                    android:scheme="https"
                />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          Without these two, a notification that arrives while the app is
          closed is posted to a fallback channel on Android 8+, so it does not
          pop up on screen and uses the wrong icon. The id must match the
          channel created in usePushNotifications.jsx ("default").
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Needed to read the real network type (Wi-Fi vs mobile data). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
      These two were unlimited. Since Android 13 they are ignored, and Google
      Play asks why an app requests them. maxSdkVersion keeps them only where
      they still do something.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      SCHEDULE_EXACT_ALARM is restricted by Google Play and needs a written
      justification; apps that only show reminders are usually rejected.
      If you do not schedule alarms to the exact minute, delete this line.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />

    <!--
      Android 11+ hides other apps unless they are declared here. This is why
      opening a downloaded PDF or APK shows your
      "no app found to open it" message even when a PDF reader is installed.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>
</manifest>
